# 星火 · 产品流程图 v1.0

> mermaid 语法，可粘贴到 https://mermaid.live 渲染，或照着画进 Visio/ProcessOn。
> 版本对齐：PRD v1.0（一期全量交付，2026-08-30）。

## 1. 用户主流程（App / 网页内）

```mermaid
flowchart TD
    A[打开应用] --> B{随机抽一句完整句<br/>句号单位 · 不重复上一句}
    B --> C[首页<br/>句 + 作品名 + 中文日期]
    C -->|换一句 · 右侧主按钮| B
    C -->|搜索 · 左侧次按钮| D[输入诗句/诗题]
    D --> E{本地命中?}
    E -->|是| F[结果列表<br/>标题 + 代表句]
    E -->|否| G[提示换词再搜]
    F --> H[点结果进详情]
    C -->|点句子| H
    H --> I[详情页<br/>标题 · 全诗 · 背景 · 解读 · 出处]
    I -->|星标收藏| J{已收藏?}
    J -->|否| K[收藏整首]
    J -->|是| L[取消收藏]
    I -->|存图| M[生成竖屏长图<br/>canvas 两遍绘制]
    M --> N{Android 壳?}
    N -->|是| O[XingHuoBridge.saveImage<br/>存入系统相册 + Toast]
    N -->|否| P[浏览器下载 PNG]
    I -->|返回| C
    C -->|齿轮| Q[我的页]
    Q --> R[收藏列表<br/>双 Tab 全诗/单句 · 管理模式]
    Q --> S[导出收藏 → 下载 JSON 备份]
    Q --> T[导入收藏 → 选择 JSON<br/>合并去重 · 严格校验]
    Q --> U[设置<br/>字体/字号/皮肤/背景]
    U --> V[applyPrefs 即时生效<br/>Android: 状态栏随皮肤同步]
    Q --> W[关于<br/>隐私政策/帮助/GitHub]
    R --> H
    S --> Q
    T --> Q
```

## 2. 内容生产流程（编辑侧，不进 App）

```mermaid
flowchart LR
    A[三本 EPUB 解压<br/>年谱全九卷/传记/鉴赏] --> B[解析器<br/>tools/build-material.py]
    B --> C[素材库<br/>年谱条目 + 传记章节 + 鉴赏索引]
    C --> D[材料包装配<br/>按诗写作时间取同期材料]
    D --> E[AI 起草背景+解读<br/>约束：只用包内材料<br/>禁「年谱记载」字样 · 禁现编 · 缺料标注]
    E --> F[人工审稿<br/>查事实 / 禁鸡汤 / 禁出处字眼 / 后见之明独立段]
    F --> G[content/poems/&lt;id&gt;.json × 63<br/>字段级校验]
    G --> H[tools/kb.py 构建<br/>全量校验：字段完整 / 行句完整]
    H --> I[web/data/poems.js<br/>← 唯一进用户包的成品数据]
    I --> J[网页 / APK 打包<br/>make-brand.py 同步品牌 dataURL]
```

## 3. 品牌与存图管线

```mermaid
flowchart LR
    A[手迹原图<br/>星.jpg / 火.jpg] --> B[抠白底 · 染品牌红<br/>tools/make-brand.py]
    B --> C[web/brand.png<br/>星火合成 · 字距 24px]
    B --> D[web/brand-data.js<br/>dataURL 内嵌]
    C --> E[网页品牌展示]
    D --> F[Canvas 存图<br/>file:// 不污染 canvas]
    F --> G[竖屏长图<br/>皮肤+背景+星火落款+全内容]
```

## 4. Android 壳架构

```mermaid
flowchart TD
    A[MainActivity] --> B[WebView<br/>file:///android_asset/index.html]
    B --> C[assets/<br/>web/ 全量资源 11.4MB]
    A --> D[XingHuoBridge 原生桥]
    D -->|saveImage| E[MediaStore<br/>存入系统相册 · 免权限]
    D -->|setTheme| F[setStatusBarColor<br/>状态栏随皮肤融合]
    A --> G[WebSettings<br/>textZoom=100 · DOM存储 · JS 启用]
    B -->|applyPrefs| D
```

## 5. 关键决策点（面试可讲）

1. **随机还是按顺序**：随机——每个句子都有机会被看见；防重复（不连续同诗同句）
2. **换一句在右**：右利手拇指热区，主行动最易触达（48px 不缩）
3. **返回保持原句**：详情/搜索/设置返回首页不换句——「回来看刚才那句」
4. **皮肤 = 整套配色**：不做独立字体颜色功能（职责重叠，冲突无从裁决）
5. **整句一行 + 15px**：七绝七律不提前换行；词长短句自然错落；字号与主流阅读 App 一致
6. **存图竖屏长图**：手机分享载体是竖屏；皮肤/背景/手迹品牌全入图
7. **品牌 dataURL 内嵌**：file:// 下 canvas 污染 → toBlob 报错；dataURL 双端安全
8. **导出/导入严格校验**：id 在库 / 整数索引 / kind 语义 / 时间合法——坏文件不破坏收藏
9. **textZoom 固定 100**：WebView 跟随系统字体大小会放大字号——固定后与设计稿一致
10. **状态栏融合**：卡片背景色蔓延到系统状态栏，深色皮肤自动白字图标

## 6. 已落地流程（v1.1）

- **存图 / 导出 / 导入二次确认**（已实现）：存图先生成预览图（弹层居中展示），用户确认后再保存；导出/导入确认含明细——涉及存储的操作均有反悔机会：

```mermaid
flowchart LR
    A[点存图] --> B[生成预览图<br/>canvas → dataURL]
    B --> C[预览弹层<br/>图 + 保存/取消 · 居中顶层]
    C -->|保存| D[存相册/下载]
    C -->|取消| E[放弃 · 无副作用]
```

- **Android 返回手势**（已实现）：返回键/左划手势 → 页面按视图栈回退（详情→来源、设置/我的/关于→上一级、首页→退出）；弹层开着时先关弹层：

```mermaid
flowchart TD
    A[系统返回] --> B{弹层开着?}
    B -->|是| C[关闭弹层]
    B -->|否| D[按视图栈回退<br/>详情/搜索/我的/设置/关于]
    D --> E{在首页?}
    E -->|是| F[退出应用]
    E -->|否| G[返回上一视图]
```

## 7. 待改进流程（V2）

- 首页存图（截图式，无底部按钮，复用预览确认）
- 背景图上传提供截取区域（自由裁切）
- 检查更新（「关于」页手动触发，GitHub Releases）
